--- 13-06-2026 ---

- 3:40pm -
Para empezar con gdelt hay que tener estos requerimientos:
    1. Instalar Python (3.10+)
    2. Instalar la biblioteca gdelt, usando (pip install gdelt)
    3. Instalar la biblioteca geopandas, usando (pip install geopandas)

Usar la biblioteca multiprocessing
Usar el (if __name__ == "__main__") para trabajar con gdelt y el programa no se quede atrapados

- 4:08pm -
Estamos logrando sacar información de gdelt

--- 14-06-2026 ---

- 2:21pm -
Ya se logró crear un contenedor de Apache Spark, usando el comando (docker run -it)

- 5:56pm -
Hay que usar el comando (docker exec -it spark /opt/spark/bin/spark-shell) para encender nuestro contenedor de spark
También ya está el Dockerfile de mongodb y el docker-compose actualizado

- 8:50pm -
Dependencias de pip importantes para que pyspark funcione de forma remota:
    pandas
    pyarrow
    grpcio
    grpcio-status
    zstandard

- 8:53pm -
AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA
Ya funciona la conexión con el contenedor de Spark