--- 13-06-2026 ---

- 3:40pm -
Para empezar con gdelt hay que tener estos requerimientos:
    1. Instalar Python (3.10+)
    2. Instalar la biblioteca gdelt, usando (pip install gdelt)
    3. Instalar la biblioteca geopandas, usando (pip install geopandas)

Usar la biblioteca multiprocessing
Usar el (if __name__ == "__main__") para trabajar con gdelt y el programa no se quede atrapados

- 4:08pm -
Estamos logrando sacar información de gdelt

--- 14-06-2026 ---

- 2:21pm -
Ya se logró crear un contenedor de Apache Spark, usando el comando (docker run -it)

- 5:56pm -
Hay que usar el comando (docker exec -it spark /opt/spark/bin/spark-shell) para encender nuestro contenedor de spark
También ya está el Dockerfile de mongodb y el docker-compose actualizado

- 8:50pm -
Dependencias de pip importantes para que pyspark funcione de forma remota:
    pandas
    pyarrow
    grpcio
    grpcio-status
    zstandard

- 8:53pm -
AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA
Ya funciona la conexión con el contenedor de Spark

- 10:23pm -
Esto se agregó al docker-compose.yml. Muy importante para que se inicie la conexión de spark
    environment:
      - SPARK_NO_DAEMONIZE=true
    command: /opt/spark/sbin/start-connect-server.sh --master local[*]

- 10:26pm -
Esto se le agregó al environment de spark para que tenga más memoria con que trabajar
    - SPARK_DRIVER_MEMORY=4g
    - SPARK_EXECUTOR_MEMORY=4g

- 12:53am -
Estoy muy cansado, no funciona la conexión de mongodb con spark
Las dependencias de mongodb no están funcionando en el contenedor de spark

--- 16-06-2026 ---

- 1:01pm -
Ya se puede hacer la conexión de Spark con Mongodb, hay que correr el script de Python
dentro del contenedor de Spark y antes de correrlo hay que instalarle todas las dependencias
para que puede hacer

- 7:23pm -
Ya se está guardando archivos en Mongodb, con el formato correcto. Tomó bastante tiempo solucionar
problemas con las estructuras que gdelt nos daba. Más bien fueron errores propios, pero ya está
funcionando. Aún no se está guardando datos automáticamente a la base de datos, pero el loader
está funcionando automáticamente

- 12:06am -
Ya funciona cargar los parquets Ahhhhhhhhhhhhhhhhhhhhhhhhhhhhhh

--- 20-06-2026 ---

- 12:14pm -
Se puso una prueba de las queries. No parecen tener errores. Técnicamente los datos se pueden guardar
en mongodb, aunque siento que deberíamos revisar los datos

--- 21-06-2026 ---
Ya los contenedores van a probarse en la maquina de Emilio